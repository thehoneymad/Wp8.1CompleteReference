<Page xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps" 
    x:Class="MVVMMaps.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVMMaps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid>
        <Maps:MapControl Center="{Binding MyLocation}" >
            <Grid Background="#FFFF4444" Maps:MapControl.Location="{Binding MyLocation}">
                <TextBlock  Text="Test" FontFamily="20" Foreground="White" ></TextBlock>
            </Grid>
        </Maps:MapControl>
    </Grid>
</Page>
