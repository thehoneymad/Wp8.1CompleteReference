<Page xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps" 
    x:Class="MVVMMaps.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVMMaps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Main, Source={StaticResource Locator}}">
	<Page.Resources>
		<Storyboard x:Name="OpenPushPin"/>
	</Page.Resources>

    <Grid>
        <Maps:MapControl  x:Name="MainMap" Center="{Binding MyLocation}" >
            <Grid x:Name="MyLocationPushpin" Background="#FFFF4444" Maps:MapControl.NormalizedAnchorPoint="0.5,1" Maps:MapControl.Location="{Binding MyLocation}" Width="80" Height="40" >
                <TextBlock  Text="Here I Am!" FontFamily="65" Margin="10" Foreground="White" ></TextBlock>
            </Grid>
        </Maps:MapControl>
    </Grid>
    
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="Find Me!" Icon="Target" Command="{Binding GetMyLocationCommand}" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
